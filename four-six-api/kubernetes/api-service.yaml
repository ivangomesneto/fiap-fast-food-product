apiVersion: v1
kind: Service
metadata:
  labels:
    app: api-svc-foursix
  name: api-loadbalancer-svc-foursix
spec:
  ports:
  - port: 30001
    protocol: TCP
    targetPort: 80
  selector:
    #app: api-pod-foursix
    app: foursix-deployment
  type: LoadBalancer
